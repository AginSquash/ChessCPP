name: C/C++ CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v1
    - name: install sfml
      run: sudo apt-get install libsfml-dev
    - name: configure 
      run: cmake
    - name: make
      run: make
